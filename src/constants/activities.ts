export interface ActivityItem {
  _id: any;
  title: string;
  imageUrl: string[];
  shortDescription: string;
  socialContactURL: string[];
  city: string;
  tags?: string[];
  createdAt: string;
  updatedAt: string;
}
